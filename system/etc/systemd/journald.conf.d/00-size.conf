# Cap persistent journal at 500M and per-file size at 100M.
# Without this the journal grows unbounded — typical Arch desktop
# accumulates several GB of logs over months.
[Journal]
SystemMaxUse=500M
SystemKeepFree=1G
SystemMaxFileSize=100M
